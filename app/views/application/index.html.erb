<h1>RubyConf2020</h1>

<h2>Available Programs (<%= programs.count %>)</h2>
<% programs.each do |prog| %>
  <div class='program' id='prog-<%= prog.id %>'>
    <%= link_to prog.name, program_path(prog) %>
  </div>
<% end %>